{
  "hooks": {
    "afterShellExecution": [
      {
        "command": "echo \"$SHELL_COMMAND\" | grep -qE '(npm test|npx jest|npx vitest|pytest|cargo test|go test)' && curl -s -X POST http://127.0.0.1:47592/api/capture/fullscreen -H 'Content-Type: application/json' -d '{\"resolution\":\"quarter\"}' > /dev/null 2>&1 || true",
        "matcher": "npm test|npx jest|npx vitest|pytest|cargo test|go test"
      }
    ]
  }
}
